node_modules/
*.sqlite
*.sqlite-*
data/
.env
.env.local
# GitHub App の秘密鍵（ホストに置く唯一の長期秘密）
*.pem
# ホスト固有の compose 差分（例: GitHub App の pem マウント）。docker compose が自動マージする
compose.override.yaml
eval/last-run/
# Claude Code がセッション中に作るローカル設定と一時ファイル（*.tmp.<pid>.<hash>）。
# git add -A で誤コミットする事故が2度起きたため追跡対象外にする。
.claude/settings.local.json*
