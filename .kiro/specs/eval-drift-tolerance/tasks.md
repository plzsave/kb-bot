# Implementation Plan

- [ ] 1. 基盤: ケース定義の拡張とフィクスチャ索引ヘルパ
- [x] 1.1 ケース定義にフィクスチャ参照を追加し検証する
  - 生ケースと検証済みケースの双方に任意のフィクスチャ参照リストを持たせ、未指定のケースは従来と完全に同一の挙動を保つ（後方互換）
  - ケース検証時に、フィクスチャ参照が指定された場合は「文字列の配列」であることを最小限チェックし、違反は既存の軸／ゲート検証と同じ日本語エラー文で実行前に停止させる
  - 観測可能な完了条件: 既存 7 ケース（フィクスチャ未指定）が検証を無改修で通過し、`fixtures` が非配列や非文字列のケースは実行前にエラー終了する
  - _Requirements: 5.3, 5.4_
  - _Boundary: Case schema extension_

- [ ] 1.2 フィクスチャ Markdown を本番 KB と分離した索引に組み立てる手段を用意する
  - フィクスチャ Markdown 群を、本番ナレッジベースを一切開かず変更しない隔離インメモリ全文検索索引に組み立てて返す関数を新設し、テスト可能なよう公開する
  - 既存のチャンク分割・索引投入・検索の部品を再利用し、フィクスチャ本文の探索は指定基準ディレクトリ配下に限定する（該当ファイルが無ければ原因パスを添えて即エラー）
  - フィクスチャ参照の解決基準は「ケース定義ファイルのあるディレクトリ」に固定し、実行時の作業ディレクトリに依存しないようにする（設計レビュー Issue 2）
  - 観測可能な完了条件: 返る索引はチャンク数が正で、フィクスチャの語を含むクエリで当該フィクスチャがヒットし、続けて別の索引を組んでも相互に内容が混ざらない（完全隔離）
  - _Requirements: 2.1, 2.2, 2.3, 2.4_
  - _Boundary: buildFixtureDb_

- [ ] 2. 矛盾データ: フィクスチャ doc とドリフトケース
- [ ] 2.1 (P) 「古い値を述べる」フィクスチャ doc を作成する
  - 「回答キャッシュは無期限で失効しない」という古い値 X を、いかにも本物の runbook 体裁で記述する（実際のコードの現在値 24 時間＝Y は本文に含めない）
  - 質問文と全文検索でトークンが重なる語（回答キャッシュ・有効期限・TTL 等）を本文に含め、隔離索引で確実に初期コンテキストへ前置きされるようにする
  - 観測可能な完了条件: フィクスチャは eval 配下に閉じて配置され、古い値 X を明示し現在値 Y を一切含まない
  - _Requirements: 4.1, 4.3, 2.3_
  - _Boundary: Drift Case + Fixture_

- [ ] 2.2 (P) 矛盾を突くドリフトケースを評価ケース集に追加する
  - キャッシュ有効期限を根拠付きで問う質問に対し、情報源はコード・現在値 Y（24）を回答に含む・読んだコードパス（キャッシュ実装ファイル）を出典として引用、を採点条件とするケースを追加する（軸 B′ タグ付き、上記フィクスチャを参照）
  - 古い値 X の否定検出（omit）は使わず、現在値 Y の明示＋情報源コード＋コードパス引用＋出典必須の積で「コードを信頼源に採ったか」を判定する（誤 FAIL 回避の設計判断）
  - 観測可能な完了条件: 追加ケースが軸 B′ として集計対象になり、コードを読んで Y を出典付きで答えれば合格・古い値を鵜呑みにすれば Y 欠落で不合格になる採点定義になっている
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 4.1, 4.2_
  - _Depends: 1.1_
  - _Boundary: Drift Case + Fixture_

- [ ] 3. 統合: 実行ループでの検索源の切り替え
- [ ] 3.1 フィクスチャを持つケースだけ隔離索引を検索源に使う配線を入れる
  - 実行ループで、フィクスチャを持つケースは隔離索引を組み立て、初期コンテキストの全文検索とエージェントのナレッジ検索ツールの双方をその索引に向ける。フィクスチャを持たないケースは従来どおり本番共有索引を使う
  - 隔離索引はケース終了時に確実に破棄し、本番共有索引は閉じない。GitHub 未設定で SKIP されるケースでは索引を組み立てる前に SKIP する（無駄な構築を避ける）
  - 観測可能な完了条件: フィクスチャ付きケースの実行時に本番ナレッジベースが読まれず変更もされず、古いフィクスチャ本文が初期コンテキストに現れる。フィクスチャ未指定ケースの挙動は不変
  - _Requirements: 2.1, 2.4, 3.1, 5.1_
  - _Depends: 1.1, 1.2_
  - _Boundary: Loop db 分岐_

- [ ] 4. 検証: ユニットテストと回帰・型チェック
- [ ] 4.1 フィクスチャ索引とケース検証のユニットテストを追加する
  - 資格情報不要のユニットテストで、索引ヘルパがフィクスチャを retrievable にすること・2 つの索引が相互隔離であること・存在しないフィクスチャで即エラーになることを検証する
  - ケース検証が、フィクスチャ参照ありを許容し・非配列や非文字列を拒否し・フィクスチャ未指定の既存ケースを通過させることを検証する
  - 観測可能な完了条件: 追加ユニットテストが `bun test` で緑になり、隔離・retrievable・fail-fast・後方互換の各性質を実証する
  - _Requirements: 2.1, 2.2, 2.4, 5.4_
  - _Depends: 1.1, 1.2, 2.1_
  - _Boundary: buildFixtureDb, Case schema extension_

- [ ] 4.2 回帰・SKIP・型チェックの最終確認
  - 型チェックが通ること、既存ケースが無改修で採点対象のまま維持されること、本番コードを一切変更していないことを確認する
  - GitHub 未設定でドリフトケースが SKIP され、合否・軸別集計・合否ゲートの母数から除外される（誤 FAIL を出さない）ことを確認する
  - 観測可能な完了条件: `bun run typecheck` がクリーンで、GitHub 未設定実行時にドリフトケースが SKIP 表示され集計に数えられず、変更差分が eval とテストの範囲に閉じている
  - _Requirements: 3.1, 3.2, 5.1, 5.2, 5.3_
  - _Depends: 3.1_
