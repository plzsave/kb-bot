# Implementation Plan: eval-next-step

- [x] 1. eval 採点に「次の一歩」判定を追加
- [x] 1.1 期待値スキーマ・検出語彙・判定純粋関数を実装
  - `Expect` に「次の一歩必須」を宣言する任意の真偽観点を追加する
  - 「次の一歩」検出のための手掛かり語彙（複数語 OR）を、既存の出典検出規約と同じ帯に定義する（一般語単独一致を避ける複合的な手掛かり語で構成）
  - 手掛かりの欠如を日本語の fail 文配列で返す純粋関数を追加する（観点が偽/未指定なら空配列・副作用なし・入力不変）
  - 観点が真で手掛かりが無い回答に対し、他の fail と区別できる固有文言の fail が 1 件返ることを関数単体で確認できる
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 4.2, 4.3_
  - _Boundary: nextStepFails, NEXT_STEP_CUES, Expect.offersNextStep_

- [x] 1.2 判定を採点フローへ連結
  - 既存採点関数の末尾（出典判定呼び出しの隣、戻り直前）に「次の一歩」判定を連結する
  - 既存分岐（ツール使用・source・引数・本文包含/除外・出典）の判定結果を変えない
  - 観点未指定の既存ケースで採点結果が変わらないことを確認できる
  - _Requirements: 2.1, 4.1, 4.2_
  - _Boundary: evalCase_

- [x] 2. (P) 本番プロンプトに未発見時の「次の一歩」指示を追記
  - 未発見時指示（推測せず見つからない旨を述べる文）に、解決に近づく次の一歩（対象名・キーワードの補足を促す／言い換えの提案／資料追加で答えられる旨のいずれか）を 1 文で簡潔に添えるよう英語で連結する
  - [Safety]・[Output style]・[Routing]・[Audience]・GitHub ブロック・出力言語自動判別の各既存文言は変更しない（推測禁止 "do not guess" を保持）
  - システムプロンプト生成の出力に「次の一歩」を促す指示が含まれ、かつ既存の安全・出力スタイル文言が残ることを確認できる
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 4.4_
  - _Boundary: buildSystem_

- [x] 3. 軸 D の評価ケースを追加
  - ナレッジにもコードにも事実が無い「未発見」質問について、軸 D かつ「次の一歩必須」観点を立てた評価ケースを本番ケースデータに 1 件以上追加する
  - 既存 guard と同等の無根拠推測の禁止条件（禁止語の非出現）を同ケースに併記し、guard の趣旨と両立させる
  - サンプルケースデータに新観点の記載例を 1 件追従追加する
  - ケースデータが評価ハーネスのスキーマ検証（軸・ゲート・fixtures 形状）を通過することを確認できる
  - _Depends: 1.1_
  - _Requirements: 3.1, 3.4_
  - _Boundary: eval/cases.json, eval/cases.sample.json_

- [x] 4. 回帰テスト
- [x] 4.1 (P) 判定純粋関数・検出語彙・軸 D ケース存在の単体/構造テスト
  - 観点未指定で空配列、手掛かり語ありで空配列、手掛かりなしで欠如 fail 1 件、手掛かり語を含まない未発見文で誤検出しないことを検証する
  - 本番ケースデータに、軸 D かつ「次の一歩必須」観点のケースが 1 件以上存在し、無根拠推測の禁止条件を併記していることを検証する（同一テストファイルに集約し、他テストとのファイル競合を避ける）
  - 資格情報・ネットワーク不要で `bun test` から緑になることを確認できる
  - _Depends: 1.1, 3_
  - _Requirements: 2.2, 2.3, 2.4, 3.1, 3.3, 3.4_
  - _Boundary: test/kb-eval.test.ts_

- [x] 4.2 (P) システムプロンプトの追記テスト
  - システムプロンプト生成出力に「次の一歩」を促す指示が含まれ、かつ [Safety]/[Output style] の既存キーフレーズが保持されることを検証する
  - `bun test` から緑になることを確認できる
  - _Depends: 2_
  - _Requirements: 1.1, 1.4_
  - _Boundary: test/systemPrompt.test.ts_

- [ ] 5. 検証: 非回帰・型・ライブ実証
  - `bun run typecheck` が型エラーなく完了する
  - 既存の全評価ケースが無改修で PASS することをライブ実行（`bun run kb:eval`）で確認する
  - 追加した軸 D ケースで、次の一歩を提示する回答が PASS・突き放す回答が FAIL になることをライブ実行で確認する
  - 上記が満たされた状態（typecheck 緑・既存非回帰・軸 D の PASS/FAIL 挙動）を観察できる
  - _Depends: 1.1, 1.2, 2, 3, 4.1, 4.2_
  - _Requirements: 3.2, 3.3, 4.1, 4.3_
